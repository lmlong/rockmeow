# Heartbeat 任务

心跳服务每30分钟读取此文件并执行其中的指令。

## 使用说明

1. 将此文件复制到 `~/.lingguard/workspace/HEARTBEAT.md`
2. 根据需要修改任务内容
3. 首次使用日历功能时，先运行 `list_calendars` 获取日历路径

## 日程检查

检查未来1小时的日历事件，如果有即将开始的会议请提醒我。

日历路径：（首次使用需要先获取，运行 list_calendars 命令）

## 其他任务

在此添加其他需要定期执行的任务...
